import { Login } from "pages/Login/Login";

export default {
  title: "Pages/Login",
  component: Login,
};

const Template = () => {
  return <Login />;
};

export const Primary = Template.bind({});
