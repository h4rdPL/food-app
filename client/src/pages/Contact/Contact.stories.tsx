import { Contact } from "pages/Contact/Contact";

export default {
  title: "Pages/Contact",
  componenet: Contact,
};

const Template = () => {
  return <Contact />;
};

export const Primary = Template.bind({});
