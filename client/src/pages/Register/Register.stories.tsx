import { Register } from "./Register";

export default {
  title: "Pages/Register",
  component: Register,
};

const Template = () => {
  return <Register />;
};

export const Primary = Template.bind({});
