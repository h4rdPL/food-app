import { Label } from "components/atoms/Label/Label";

export default {
  title: "Atoms/Label",
  component: Label,
};

const Template = () => {
  return <Label>Zarejestruj się jako partner biznesowy</Label>;
};

export const Primary = Template.bind({});
