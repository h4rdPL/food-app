import { Span } from "styled-components";

export default {
    title: "Atoms/Span",
    component: Span
};

const Template = () => {
    return <Span>Wygląda na to, że nie jesteś zalogowany!</Span>
}

export const Primary = Template.bind({});