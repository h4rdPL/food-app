import { Info } from "./Info";

export default {
    title: "Organisms/Info",
    component: Info
}

const Template = () => {
    return (
        <Info />
    );
}

export const Primary = Template.bind({})