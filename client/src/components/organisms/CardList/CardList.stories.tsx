import { CardList } from "./CardList";

export default {
  title: "Organisms/Card Lists",
  component: CardList,
};

const Template = () => {
  return <CardList />;
};

export const Primary = Template.bind({});
